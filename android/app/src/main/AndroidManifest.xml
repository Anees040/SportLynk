<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- S.7 Wave C. Android 13 (API 33) made notifications an explicit runtime
         permission: without this line the request PushService makes is denied
         instantly and silently, with no dialog and no error, and the phone simply
         never buzzes. Declaring it does not grant it -- the user is still asked
         once, by `FirebaseMessaging.requestPermission()`. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <application
        android:label="SportLynk"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- How a TRAY TAP reaches Dart. `pushService.buildMessage` sets
                 clickAction/click_action to FLUTTER_NOTIFICATION_CLICK on every
                 message; this filter is what routes that intent back into this
                 activity so firebase_messaging can hand it to
                 `onMessageOpenedApp` (app backgrounded) or `getInitialMessage()`
                 (app killed). Without it the banner opens the app on its home
                 screen and the deep link is lost -- which is precisely the
                 "the tap does nothing" failure this wave exists to close. -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- The channel every FCM message is delivered on. Named here so the
             Firebase SDK uses it instead of creating its own
             `fcm_fallback_notification_channel` at DEFAULT importance, which would
             throw away the high-priority heads-up behaviour the server asks for.
             The channel itself is CREATED in MainActivity.kt -- this meta-data only
             names it, and the two strings plus `ANDROID_CHANNEL` in
             backend/src/services/pushService.js must all match. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="sportlynk_default" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Scout's map cards hand a `geo:` pin to whatever the phone uses for maps.
             Without this, Android 11+ hides every maps app from us and the pin falls
             back to a browser page. Declared as a query only — nothing is launched
             that the user did not tap. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="geo"/>
        </intent>
    </queries>
</manifest>
